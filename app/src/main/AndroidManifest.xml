<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hiveapp_login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hive"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_hive_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HiveApp_login"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity"
            android:label="@string/login_toolbar_title"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.login.RegisterActivity"
            android:label="Join to Hive"
            android:screenOrientation="portrait" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.chat.ChatActivity"
            android:label="Enjoy Hive"
            android:screenOrientation="portrait" />
    </application>

</manifest>